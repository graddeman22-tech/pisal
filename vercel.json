{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/pisal-store run build",
  "outputDirectory": "artifacts/pisal-store/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
